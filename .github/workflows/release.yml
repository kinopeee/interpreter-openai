name: release

on:
  push:
    tags: ['windows-v*']
  workflow_dispatch:
    inputs:
      tag:
        description: '既存の windows-v* タグ名 (例: windows-v0.1.0)。タグは事前に push 済みであること'
        required: true

permissions:
  contents: write

jobs:
  windows:
    name: publish and release (win-x64)
    runs-on: windows-latest
    steps:
      # 手動実行でもビルド対象と公開タグを一致させるため、checkout 前にタグを確定する。
      - name: Resolve release tag
        id: release
        shell: pwsh
        run: |
          $tag = if ($env:GITHUB_REF -like 'refs/tags/*') {
            $env:GITHUB_REF -replace '^refs/tags/', ''
          } else {
            '${{ inputs.tag }}'
          }
          if ([string]::IsNullOrWhiteSpace($tag)) { throw 'release tag is empty' }
          if ($tag -notmatch '^windows-v') {
            throw "release tag must start with 'windows-v': $tag"
          }
          "tag=$tag" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.release.outputs.tag }}
          persist-credentials: false

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: windows/global.json

      - name: Test
        shell: pwsh
        run: dotnet test windows/RealtimeTranslator.slnx --configuration Release

      - name: Restore for publish
        shell: pwsh
        run: dotnet restore windows/RealtimeTranslator.slnx --runtime win-x64

      - name: Publish self-contained app
        shell: pwsh
        run: ./scripts/publish-windows.ps1 -Runtime win-x64 -NoRestore

      - name: Package
        id: package
        shell: pwsh
        env:
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
        run: |
          $zip = "artifacts/RealtimeTranslator-$env:RELEASE_TAG-win-x64.zip"
          Compress-Archive -Path 'artifacts/RealtimeTranslator-win-x64/*' -DestinationPath $zip
          $hash = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant()
          "$hash  $([System.IO.Path]::GetFileName($zip))" | Out-File "$zip.sha256" -Encoding ascii
          "zip=$zip" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Create release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
          ZIP_PATH: ${{ steps.package.outputs.zip }}
        run: |
          # --verify-tag: 未作成タグを default branch tip に勝手に作らない。
          # checkout 済みのタグとバイナリが一致していることを前提にする。
          gh release create $env:RELEASE_TAG "$env:ZIP_PATH" "$env:ZIP_PATH.sha256" `
            --verify-tag `
            --title "Realtime Translator for Windows $env:RELEASE_TAG" `
            --notes-file .github/release-notes-windows.md
